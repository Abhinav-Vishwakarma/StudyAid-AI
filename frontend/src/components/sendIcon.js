import React from 'react';

function SendIcon() {
  return (
    <button type="submit" className="submit-button">
      {/* Replace with your actual send icon (e.g., an SVG) */}
      <svg width="24" height="24" viewBox="0 0 24 24"> 
        {/* ... your send icon SVG code ... */} 
      </svg>
    </button>
  );
}

export default SendIcon;